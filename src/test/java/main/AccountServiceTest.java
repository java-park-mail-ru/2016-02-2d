package main;

import org.junit.Test;

import static org.junit.Assert.*;

public class AccountServiceTest {
    // All methods are quite simple and consist of 2-4 lines, where no error is possible. Except if java.collections.* will go wrong...
}